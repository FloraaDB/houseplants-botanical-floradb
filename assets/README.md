# Assets

Marketing images referenced from the top-level `README.md`, e.g.:

```markdown
![FloraDB data preview](assets/kaggle-cover.png)
```

Suggested assets:
- `kaggle-cover.png` — hero/cover image (herbarium specimen style) used in the README and Kaggle.
- `preview.png` — a screenshot of the dataset (a few rows in a spreadsheet or DB browser).
- `landing.png` — a screenshot of the landing page.

(GitHub renders the top `README.md`, so a hero image here makes the repo landing far more compelling.)
